# SkillForge · 技能精炼台 —— 黑客松解决方案设计报告

> 场景：深度研究 ｜ 主题：面向 AI 工作台的 Skill 资产格式治理与 Token 优化
> 交付物：一个可本地部署的「个性化 Skill 资产优化工作台」（可视化 + 一键部署）

---

## 1. 问题定义与根因分析

### 1.1 现象

在长期的 AI 工作台使用中，用户沉淀了大量**自定义 Skill（`SKILL.md`）**。通过对真实工作环境抽样（如 `plan-tracker`、`materials-to-pptx`）的核查，我们发现这些资产普遍呈现三类问题：

| 维度 | 典型表现 | 样例 |
|------|----------|------|
| **格式混乱** | 字段命名/数量不统一：`description`/`description_zh`/`description_en`/`version`/`display_name`… 并存；`name` 与目录名不一致 | `plan-tracker` 含 9 个 frontmatter 字段 |
| **表述冗余** | description 堆砌长句、营销废话（强大的/一站式/极致）、重复罗列能力 | `plan-tracker` description 达 **175 token** |
| **语义不统一** | 人称混用（"当用户…"/"使用本技能"/"Use this skill"）、缺少触发场景、单技能承载多职责 | 中英文与祈使/第三人称混排 |

### 1.2 根因（关键洞察）

`SKILL.md` 的 `name` 与 `description` 会被**常驻注入到每一轮对话的「可用技能列表」**（这是 Agent 决定调用哪个 Skill 的唯一依据）。由此产生两个连锁后果：

1. **无效 Token 持续放大**：description 每多 1 个冗余 token，**每一个对话轮次**都重复付出该成本。这是「一次写入、永久付费」的复利式浪费。
2. **调度识别偏差与协同紊乱**：语义不统一使 Agent 难以准确匹配需求 → 误触发 / 漏触发 / 多技能职责重叠 → 协同效率下降。

### 1.3 量化模型

```
单技能每轮节省      Δ_i = max(0, desc_tokens_before_i − desc_tokens_after_i)
会话累计节省        Σ_session = Σ_i Δ_i × N_turns
```

**示例**：若存在 20 个技能、平均各压缩 30 token，则每轮省 600 token；一个 1000 轮的会话即可节省约 **60 万 token**。这正是本方案的价值锚点。

---

## 2. 现状调研（Research）

- **官方 Skill 规范**（`skill-creator` 内置规范）明确：Skill 采用三级加载——① metadata（name+description）**始终常驻上下文（~100 词）**；② SKILL.md 正文（触发时加载）；③ 捆绑资源（按需）。这从机制上印证了 **description 是 Token 优化的首要杠杆**。
- **规范原则**：要求 `description` 用第三人称、明确"做什么 / 何时用"；正文应保持精简，细节移入 `references/`。但**缺少机器可校验的标准与自动化工具**。
- **市场缺口**：现有工具能"创建/打包" Skill，却没有"治理存量 Skill 资产格式、压缩冗余、追踪调用效果"的轻量工作台。SkillForge 正填补此缺口。

---

## 3. 解决方案总览

**产品定位**：一个以小型个性化工作台形式部署的 Web 应用，让个人开发者用可视化界面完成 Skill 资产的**统一格式校验 → 语义清洗 → 冗余压缩 → 调用效果追踪**闭环。

四大能力：

1. **统一格式校验**：按《SkillForge 标准规范 v1.0》做三轴（格式 / 语义 / 冗余）自动体检，输出健康度评分与可执行的修复建议。
2. **语义清洗**：规则引擎把混乱描述归一为标准模板 `<用途一句话>。触发场景：<a>；<b>；<c>。`，移除填充词、重复字段；可选接入 LLM 做语义级重写。
3. **冗余压缩**：以 Token 预算（目标 ≤60，硬上限 120）压减 description，前后对比量化节省；支持一键写回（自动备份原文件）。
4. **调用效果追踪**：SQLite 记录每次优化/应用事件，看板展示累计节省、每轮常驻节省、节省趋势与技能排行。

### 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│  浏览器（可视化工作台：概览 / 校验 / 清洗 / 追踪 / 数据看板）   │
└───────────────┬─────────────────────────────────────────────┘
                │  REST /api
┌───────────────▼─────────────────────────────────────────────┐
│  FastAPI 服务 (skillforge.server)                            │
│  ├─ 解析层  skill_parser   递归扫描 SKILL.md + YAML frontmatter│
│  ├─ 校验层  validator      三轴规则集 + 健康度评分             │
│  ├─ 清洗层  cleaner        规则清洗 + 触发场景提取 + LLM 插件  │
│  ├─ 计量层  tokenizer      tiktoken(cl100k_base) / 启发式回退  │
│  └─ 追踪层  tracker        SQLite 事件表 + 聚合看板           │
└───────────────┬─────────────────────────────────────────────┘
                │
   ┌────────────┴─────────────┐
   ▼                          ▼
用户 skills 目录          SQLite (data/skillforge.db)
(~/.workbuddy/skills)
```

---

## 4. 标准规范设计（SkillForge 标准 v1.0）

- **模板**：`name: <kebab-id>` + `description: <用途一句话>。触发场景：<a>；<b>；<c>。`
- **字段约束**：`name` 必须 kebab-case 且与目录名一致；`description` 单一职责、含明确触发场景、Token ≤ 预算；不鼓励 `description_zh/_en` 等重复语义字段。
- **三轴校验**（8 条规则，详见 `spec/STANDARD.md`）：
  - 格式：YAML 可解析、必填字段、命名/目录一致
  - 语义：含用途与触发场景、聚焦单职责
  - 冗余：Token 不超预算、无营销废话、无重复短语/字段、正文精简
- **健康度**：基准 100，error −18 / warning −6 / info −2；<60 异常，60–89 待优化，≥90 合规（描述超硬上限即便其它满分也不评"合规"）。

---

## 5. 核心算法

### 5.1 清洗流水线（两阶段）

- **Stage 1 规则（确定性、无需 LLM）**：
  1. `name` 归一为 kebab-case；
  2. 描述提取「用途 + 触发场景」——模式识别 `当用户X时` / `本技能用于X` / 显式 `触发场景：`；
  3. 去填充词（强大的/一站式/seamless…）、去括号示例、去「适用于/用于」等前缀；
  4. 触发词拆分（`；;，,、+/`）、去重、过滤残缺片段（含「等/场景/（」的碎片）、保留 Top-3；
  5. 按 Token 预算从尾部压缩，保用途、留触发场景；
  6. 移除 `description_zh/_en` 等重复语义字段。
- **Stage 2 LLM（可选）**：配置 `LLM_API_URL/KEY` 后做语义级重写，进一步压到目标预算并修正表述。

### 5.2 Token 计量

优先 `tiktoken/cl100k_base`；缺失时回退到 `max(1, cjk/1.6 + other/4)` 启发式，保证零原生依赖也可运行（已实测：无 Rust 编译环境下启发式兜底生效）。

---

## 6. 系统实现（已落地可运行）

**目录结构**

```
skill-forge/
├── skillforge/        # 后端（config/tokenizer/spec/parser/validator/cleaner/tracker/server）
├── frontend/          # 可视化工作台（原生 HTML/CSS/JS，零构建）
│   ├── index.html  style.css  app.js
├── spec/STANDARD.md   # 标准规范文档
├── Dockerfile  docker-compose.yml  run.sh  requirements.txt  README.md
```

**后端**：FastAPI，模块清晰；REST 接口含 `/api/skills`、`/api/skills/{id}`、`/api/clean`、`/api/apply`、`/api/stats`、`/api/tracking/{id}`、`/api/spec`。`/api/apply` 含**防御性校验**（拒绝缺少 frontmatter/name/description 的退化写入，写前自动 `.bak` 备份），避免误写损坏技能文件。

**前端**：单页工作台，左侧技能资产列表（健康状态徽标），右侧四标签页（概览 / 校验 / 清洗 / 追踪），外加「数据看板」展示 KPI、节省趋势、技能排行与标准规范。无需打包、无 CDN 依赖。

**数据**：SQLite `events` 表记录 `skill_id/action/tokens_before/tokens_after/saved/ts`，支撑趋势与排行聚合。

---

## 7. 实测效果（基于真实样本）

| 技能 | 清洗前 | 清洗后 | 节省 | 说明 |
|------|--------|--------|------|------|
| `plan-tracker` | 175 token | 58 token | **67%** | 移除 `description_zh/_en`，归一为「用途+SMART澄清；OKR月周拆解；B基础」 |
| `materials-to-pptx` | 134 token | 35 token | **74%** | 去括号示例与「当用户…使用本技能」冗余框架，保留触发场景 |

> 两个样本平均减少约 **118 token/技能**。按第 1.3 节模型，若工作台有 20 个类似技能，每轮约省 2360 token，千轮会话省约 **236 万 token**。

---

## 8. 部署与落地（个人开发者 5 分钟上手）

- **Docker**：`docker compose up -d` → 打开 `http://localhost:8000`（挂载本机 `~/.workbuddy/skills` 只读、数据持久化）。
- **本地一键**：`chmod +x run.sh && ./run.sh`（自动建虚拟环境、装依赖、启动）。
- **手动**：`pip install -r requirements.txt` + `uvicorn skillforge.server:app`。
- 配置项：`SKILLS_DIRS`（扫描目录）、`DATA_DIR`（数据）、`LLM_API_URL/KEY/MODEL`（可选语义重写）。

---

## 9. 黑客松演示脚本（建议 5 分钟）

1. **开场**（30s）：展示问题——`plan-tracker` 的 175-token description 每轮都在"烧钱"，且中英文/人称混乱导致调度不准。
2. **校验**（1min）：打开工作台 → 左侧列出全部 Skill 与健康状态 → 点 `plan-tracker` → 「校验」页展示 8 条规则命中。
3. **清洗**（1.5min）：切「清洗」→ 运行清洗 → 展示前后对比（175→58，省 67%）与变更清单（去重字段、归一模板）→ 点「应用并写回」（自动 `.bak`）。
4. **看板**（1min）：切「数据看板」→ KPI（已优化技能 / 累计节省 / 每轮常驻节省 / 千轮节省）、趋势图、技能排行。
5. **扩展**（1min）：提及 LLM 语义重写模式、SQLite 追踪闭环、Docker 一键部署。

---

## 10. 局限与未来工作

- **规则清洗上限**：纯规则对高度口语化/嵌套描述的重写能力有限；接入 LLM 后质量显著提升（已有插件位）。
- **效果追踪增强**：当前追踪优化动作事件；未来可对接 Agent 调用日志（hook），统计真实「调用命中率/误触发率」反哺规范迭代。
- **批量治理**：支持多选技能一键批量清洗 + 差异 PR，适配团队级 Skill 资产治理。
- **规范自适应**：基于追踪数据动态收紧/放宽预算，形成「规范—清洗—度量—优化」飞轮。

---

## 附：API 清单

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 + 分词器/技能数 |
| GET | `/api/spec` | 标准规范 v1.0 + 规则集 |
| GET | `/api/skills` | 技能列表 + 校验摘要 |
| GET | `/api/skills/{id}` | 单技能详情 + 校验结果 |
| POST | `/api/clean` | 清洗（可选 `use_llm`） |
| POST | `/api/apply` | 写回文件（含备份 + 防御校验） |
| GET | `/api/stats` | 全局统计 + 趋势 + 排行 |
| GET | `/api/tracking/{id}` | 单技能调用效果 |
